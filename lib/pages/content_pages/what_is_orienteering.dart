import 'package:flutter/material.dart';

class WhatIsOrienteering extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Here is information about what orienteering is.'),
    );
  }
}