import 'package:flutter/material.dart';

class CompassUse extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Here is information about using compass.'),
    );
  }
}